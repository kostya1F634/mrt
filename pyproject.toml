[project]
name = "mrt"
version = "0.1.0"
description = "Mouse rotation calibration tool"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "flet[all]>=0.84.0",
    "evdev>=1.9.3; sys_platform == 'linux'",
    "pynput>=1.8.1; sys_platform == 'win32'",
]
